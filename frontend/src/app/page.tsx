import { LandingPage } from '@/features/Landing/LandingPage';

/**
 * Route entry only. All UI lives in the feature module and all logic in its
 * hook — see `src/features/Landing/`.
 */
export default function Page() {
  return <LandingPage />;
}
